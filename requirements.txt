# 설치가 성공한 뒤 아래 명령으로 정확한 버전을 고정하세요:
#   pip freeze > requirements.lock.txt

# --- LangChain 코어 ---
langchain>=0.3.20,<0.4
langchain-core>=0.3.60,<0.4
langchain-community>=0.3.20,<0.4
langchain-text-splitters>=0.3.5,<0.4

# --- 모델 연동 ---
langchain-anthropic>=0.3.15,<0.4    # 답변 생성 (claude-opus-5)
langchain-openai>=0.3.0,<0.4        # 임베딩 (text-embedding-3-small)

# --- 벡터스토어 ---
langchain-chroma>=0.2.0,<0.3
chromadb>=0.5.0,<2

# --- 검색 (3단계) ---
rank_bm25>=0.2.2                    # BM25 키워드 검색 (순수 파이썬)
sentence-transformers>=3.0          # 크로스인코더 리랭킹
# torch는 CPU 전용 휠을 별도 인덱스에서 설치하세요 (기본 휠은 CUDA가 붙어 큽니다):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- 유틸 ---
python-dotenv>=1.0
PyYAML>=6.0
