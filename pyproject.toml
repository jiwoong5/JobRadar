[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "jobradar"
version = "0.1.0"
description = "채용 공고 검색 어시스턴트 (LangChain RAG 학습 프로젝트)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
